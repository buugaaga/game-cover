// import React from 'react'
// import '@testing-library/jest-dom/extend-expect'
// import { render, screen } from '@testing-library/react'

// import Shirt from '../components/Shirt' 
 
// test('shows the shirt when the isFull is true', () => {
//   render(<Shirt isFull={true} />)
//   expect(screen.queryByText('Поставить рубашку')).toBeInTheDocument()
// })

// test('hide the shirt button when the isFull props is false', () => {
//   render(<Shirt isFull={false} />)
//   expect(screen.queryByText('Поставить рубашку')).toBeNull()
// })
